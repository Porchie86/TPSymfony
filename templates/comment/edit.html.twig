{% extends 'base.html.twig' %}

{% block title %}modification commentaire #{{ comment.id }} - Bucket-List{% endblock %}

{% block body %}
    <div class="card mb-3">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_row(form.content) }}
            {{ form_row(form.note) }}
            <button type="submit" class="btn btn-primary">Envoyer</button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
